# Alarm-Clock
This project implements a **digital alarm clock using Verilog HDL**. It generates real-time clock signals, allows initialization of the current time, supports setting and enabling/disabling an alarm, and provides a stop function for the alarm output. The design demonstrates digital clock control logic and alarm triggering functionality.
